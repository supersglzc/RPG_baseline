command:
  - ${env}
  - ${interpreter}
  - ${program}
  - ${args_no_hyphens}
method: grid
metric:
  goal: maximize
  name: reward
parameters:
  --seed:
    values:
      - 1
      - 2
      - 3
      - 4
      - 5
  --env_name:
    values:
      - antmaze-v3
  --exp:
    values:
      - dense
program: mbrpg.py

